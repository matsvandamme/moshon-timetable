// Copy this file to wifi_secrets.h and fill in your home Wi-Fi credentials.
// wifi_secrets.h is gitignored — never commit real credentials.
#pragma once

#define WIFI_SSID      "your-ssid-here"
#define WIFI_PASSWORD  "your-password-here"

// Optional: comma-separated list of NTP servers. Defaults are fine for Belgium.
#define NTP_SERVER_1   "be.pool.ntp.org"
#define NTP_SERVER_2   "pool.ntp.org"
